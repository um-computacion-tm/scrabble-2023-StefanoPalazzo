version: 2.1

jobs:
  build:
    docker:
      - image: cimg/python:3.10.1
    steps:
      - checkout
      - run:
          name: Running Tests
          command: coverage run -m unittest && coverage xml && coverage report -m
